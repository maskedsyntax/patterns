output: dist/
releases:
  - name: release
    jobs:
      - name: macos-pkg
        package:
          platform: macos
          target: pkg
      - name: macos-dmg
        package:
          platform: macos
          target: dmg
      - name: linux-deb
        package:
          platform: linux
          target: deb
